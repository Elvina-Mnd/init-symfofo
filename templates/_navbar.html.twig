<nav class="navbar navbar-expand-md navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <a class="navbar-brand" href="{{path('home')}}"> <img src="https://zupimages.net/up/21/05/qo8e.png" alt="logo" width="20%"> </a>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{path('shop')}}">Boutique</a>
      </li>
      {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item">
        <a class="nav-link" href="{{path('admin')}}">Administration</a>
      </li>
      {% endif %}
      <li class="nav-item dropleft d-none d-sm-block">
						<a class="nav-item btn btn-dark text-center nav-link-button" id="dropdown1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							<i class="fas fa-user"></i>
						</a>
						<div class="dropdown-menu dropdown-menu-left text-center">
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{path('account')}}">Mon compte</a>
							<br>
							<a href="{{path('commands_all')}}">Mes achats</a>
							<br>
							<a href="{{path('app_logout')}}">
								<i class="fas fa-sign-out-alt"></i>
								Se déconnecter</a>
       {% else %}
       <a class="nav-link" href="{{path('login')}}">Se connecter</a>
       <a class="nav-link" href="{{path('register')}}">S'inscrire</a>
       {% endif %}
       </div>
      </li>
      <li class="nav-item">
       {% if app.session.get('panier') %}
      <a class="nav-link nav-bar-white title-navbar" href="{{path('cart_index')}}">
        <!--ajouter lien panier-->
        <i class="fas fa-shopping-cart"></i>
        <span class="badge badge-danger badge-nav">{{ app.session.get('count') }}</span>
      </a>
      {% else %}
      <a class="nav-link nav-bar-white title-navbar" href="{{path('cart_index')}}">
        <!--ajouter lien panier-->
        <i class="fas fa-shopping-cart"></i>
        </span>
      </a>
      {% endif %}
      </li>
      {# <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Dropdown link
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="#">Action</a>
          <a class="dropdown-item" href="#">Another action</a>
          <a class="dropdown-item" href="#">Something else here</a>
        </div>
      </li> #}
    </ul>
  </div>
</nav>